<div class="row">
    {% for item in items %}
    <div class="col col-xs-6">
        <div class="info-box">
            <h2>
                {{ item.title }}
            </h2>
            <h3>
                {{ item.subtitle }}
            </h3>
            <button>
                بیشتر بدانیم
            </button>
        </div>
    </div>
    {% endfor %}

</div>

<img class="center-image" src="{{ asset("bundles/darkishwebsite/images/256/cadr-audio.png") }}" />